/build/
/build_deploy/
/deploy/
/installer/
/logs/
*.user
*.pyc
__pycache__/

# Binarios pesados / runtime embebido (no versionar)
/python_runtime/
/thirdparty/

# Artefacto generado: instalador.bat lo reescribe entero en cada corrida, con
# `echo` de cmd.exe, o sea en CRLF. Versionarlo dejaba el repo desalineado
# contra la politica de line endings despues de cada build, en silencio. La
# receta que si se versiona es instalador.bat; esto es su salida.
/*_installer.iss

# Icono de carpeta en Windows
desktop.ini

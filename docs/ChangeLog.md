v0.121: Al primer arranque en una maquina sin `window_geometry.ini`, `MainWindow::loadWindowSettings()` caia a `QSettings s;` para migrar la geometria de una instalacion vieja: ese constructor abre con `NativeFormat`, que en Windows es `RegCreateKeyEx` y crea `HKCU\Software\LGA\LGA SeqChecker` con solo LEER, aunque la clave nunca hubiera existido. Ahora esa rama solo se toma si la clave YA existe (chequeo de solo lectura con `RegOpenKeyExW`); si no existe, no se toca el registro para nada. El guardado de geometria tampoco cae mas a esa clave en Windows (en mac, plist via `NativeFormat`, sigue igual). El desinstalador borra la clave y `Software\LGA` si queda vacia. [ Settings - La migracion de geometria ya no crea la clave vieja del registro ]

v0.120: El icono de la app usaba las tintas CMY puras (azul #000083, verde #00a500, etc.), un contraste distinto al del resto de las apps LGA. Se levantan con la formula que ya usa el cuerpo #262626 (nuevo = 38 + viejo x 217/255 por canal, solo donde la tinta sigue pura), igual que PipeSync: el azul queda en #262695 y el verde en #26b226. Se corrigen el `.ico`, el `.png` y el `.svg` vigentes en `resources/icons/` y sus maestros en `resources/icons/Alta/`; las etapas intermedias `_FlattenTransparency.svg` y `_Original.svg` no se tocan. [ SeqChecker - Las tintas del icono pasan al levantado de PipeSync ]

v0.119: El cuerpo oscuro del icono de la app usaba #2b2b2b, un tono distinto al del resto del pack (PipeSync, MediaTools, etc.), que ya estandarizo en #262626. Se recolorean el `.ico`, el `.png` y el `.svg` vigentes en `resources/icons/` y sus maestros en `resources/icons/Alta/`, preservando el desregistro CMY de las planchas de color. Medido antes y despues: el cuerpo pasa de #2b2b2b a #262626 sin tono residual. Verificado en hoja comparativa a 1024 y 32 px. [ SeqChecker - El cuerpo del icono pasa a #262626 ]

v0.118: SeqChecker es de instancia unica, pero `compilar.bat` y el instalador cerraban solo la copia que iban a pisar: la instalada o la de otro checkout seguian abiertas junto a la nueva, cada una con su `python.exe` escaneando. Ahora `compilar.bat` sin `--no-run` y `PrepareToInstall` cierran TODAS las copias de la app y el `python.exe` de cada una, elegido por la carpeta de su instancia y nunca por nombre, con `tools\close_by_path.ps1` rev 4 de la Base (`-AllInstances -Helpers python.exe`). `compilar.bat --no-run` sigue cerrando solo `build\SeqChecker.exe`, y el desinstalador solo lo que corre desde `{app}`. Probado con procesos senuelo de dos apps que comparten `python.exe` y con un `.iss` minimo de prueba. [ SeqChecker - Compilar e instalar cierran todas las instancias ]

v0.117: El instalador cerraba por ruta solo `SeqChecker.exe`. El `python.exe` del runtime de la instalacion, que la app lanza para escanear secuencias, podia quedar huerfano y seguir escaneando, reteniendo archivos de `{app}`: Inno avisaba "archivo en uso" al reinstalar y el desinstalador no podia borrarlos. Ahora `PrepareToInstall` e `InitializeUninstall` cierran tambien el `python.exe` que corre desde `{app}`, con `-Tree` para bajar sus hijos de la misma carpeta. El python de otras apps o de un build no se toca. Probado con un `.iss` minimo de prueba y procesos senuelo. [SeqChecker - El instalador cierra tambien el python de la app]

v0.116: El instalador no cerraba la app: con una copia abierta desde la carpeta de destino, Inno avisaba "archivo en uso" al reinstalar, y al desinstalar el exe quedaba sin borrar. Ahora el `.iss` que genera `instalador.bat` cierra por ruta, con el bloque de la Base (`Doc_Instaladores_Inno.md` 5.1): `PrepareToInstall` extrae `tools\close_by_path.ps1` y cierra solo el `SeqChecker.exe` que corre desde `{app}`, y `InitializeUninstall` hace lo mismo con la copia del script que queda instalada en `{app}\tools`. Un build, otro checkout u otra instalacion siguen abiertos. Probado con un `.iss` minimo de prueba y procesos senuelo. [SeqChecker - El instalador cierra la app por ruta]

v0.115: `compilar.bat`, `deploy.bat` e `instalador.bat` cerraban por nombre todos los `SeqChecker.exe` abiertos, incluida la copia instalada con la que se estuviera revisando una secuencia. Ahora cada uno cierra solo la copia que corre desde su propio arbol (`build\`, o `deploy\` y `build_deploy\`), por la ruta real del proceso (`tools\close_by_path.ps1`, copia de la Base). `deploy.bat` cierra antes de borrar `deploy\` y no despues. Ademas `compilar.bat` y `deploy.bat` toman su carpeta antes de leer los argumentos: `shift` corria tambien el parametro cero y, llamados con argumentos desde otra carpeta, resolvian rutas contra la carpeta actual. [SeqChecker - Cerrar la app por ruta, no por nombre]

v0.114: SeqChecker decidia si corria desde la carpeta de compilacion por el nombre de la ruta: instalado en una carpeta que contuviera "build" (por ejemplo `D:\builds\SeqChecker`), buscaba el runtime de Python, los scripts, `oiiotool`/`ffprobe` y la config una carpeta mas arriba, afuera de la instalacion, y escribia los logs ahi. Ahora lo decide lo que hay en disco, con el criterio de la Base: un `CMakeCache.txt` en la carpeta del exe o hasta dos niveles arriba, y arriba la raiz del repo (`CMakeLists.txt` y `py_scr/`). Comparado en solo lectura contra todos los builds, deploys e instalaciones reales de la maquina: la respuesta no cambia en ninguno de SeqChecker. [SeqChecker - Deteccion de build por disco]

v0.113: Desinstalar dejaba la carpeta de la app a medio borrar. Inno Setup solo saca los archivos que copio el instalador, asi que `logs/` —que la app crea en cada arranque— y los `__pycache__` que Python deja adentro de `py_scr/` y `python_runtime/` quedaban huerfanos, y con eso la carpeta de instalacion tampoco se borraba. El bloque que genera el `.iss` en `instalador.bat` suma una seccion `[UninstallDelete]` con `Type: filesandordirs` para esas tres carpetas; ninguna guarda trabajo del usuario, y la config chica sigue viviendo en AppData, que sobrevive a proposito a una reinstalacion. Criterio general en `../LGA_Base_QT_C_Py/docs/Doc_Rutas_Instalacion.md`. Verificado compilando el `.iss` generado con ISCC. [SeqChecker - El desinstalador se lleva lo que la app escribio en su carpeta]

v0.112: `compilar.bat --no-run` compilaba y abria la app igual. Los flags se leian con `( set NO_RUN=true & shift ...)`, y `cmd.exe` guarda el espacio que queda antes del `&`: la variable valia "true " y nunca era igual a "true". Pasaba con todos los flags del bloque, incluidos `--wait`, `--release` y `--force-clean`. Se asignan con `set "VAR=valor"`, que no arrastra el espacio. Verificado: con `--no-run` compila, avisa que omite la ejecucion y no queda ningun proceso abierto. [SeqChecker - Los flags de compilar.bat vuelven a funcionar]

v0.111: Se saca `georgia.ttf` del binario. La fuente se registraba al arrancar y viajaba en el `.qrc`, pero ninguna parte de la UI la pide por nombre, asi que no cambia nada de lo que se ve. Georgia es de Microsoft y su licencia no permite redistribuirla dentro de una app, que es lo que hacia el ejecutable. Mismo cambio que ya se hizo en MediaTools y FileManager S3. Compilado sin errores. [SeqChecker - Fuera Georgia, que no se podia redistribuir]

v0.110: `compilar_dev.bat` era el unico script de build de Windows y ya arrastraba los problemas medidos en otras apps LGA: copiaba dependencias de runtime detras de centinelas de grupo con `2>nul` (una DLL sola tapaba que faltaran las demas), el guard de generador comparaba una variable dentro del mismo bloque `if` donde se escribia -cmd.exe la expande al parsear, asi que siempre leia vacio-, y no tenia `--wait` para ver el exit code de un crash al arranque. Se renombra a `compilar.bat` (motor unico, sin `_dev`) y se reescribe con el patron verificar->copiar->windeployqt->verificar->abortar, lista de dependencias derivada del `find_package(Qt6 ...)` real (sin `Qt6Network` ni TLS, que eran residuo), guard de generador en subrutina, y `--wait` con propagacion de exit code. Se actualizan las referencias en `README_APP.md` y en los `.mdc` de reglas de build. [SeqChecker - Un solo motor de compilacion en Windows: compilar.bat]

v0.109: `sync_version.bat --check` no chequeaba: corria el sync. El wrapper acepta cualquier flag y se lo pasa tal cual al `.py`, que no lo conocia, asi que un comando que parece de solo lectura **bumpeaba la version y reescribia el `CMakeLists.txt`**. Solo no mordia porque ya estaban sincronizados; desfasados, escribia. Es el mismo bug que LinkRedirector arreglo en su v0.163, portado tal cual. Verificado: sincronizado da rc=0; con el `VERSION` desincronizado a mano da rc=1, nombra la superficie atrasada y **no escribe nada**. Se comenta ademas la linea `*.iss text eol=lf` del `.gitattributes`, que quedo huerfana al dejar de versionarse el `.iss` generado: la declaracion sigue siendo la correcta para un `.iss` escrito a mano, pero sin la aclaracion parecia la regla que el caso anterior demostro que no alcanzaba. [SeqChecker - sync_version --check deja de escribir]

v0.108: El `.iss` del instalador deja de versionarse. Lo genera `instalador.bat` entero en cada corrida, con `echo` de `cmd.exe`, que escribe CRLF siempre — contra el `eol=lf` que declara el `.gitattributes`. El desajuste no lo mostraba `git status` (git normaliza al leer), asi que el repo quedaba desalineado en silencio despues de cada build y solo lo veia una herramienta de line endings. Es un artefacto 100% derivado: la version sale de `VERSION`/`CMakeLists.txt`, nunca del `.iss`, y sacarlo no cambia el resultado de `sync_version`: da exactamente lo mismo con y sin el archivo, verificado moviendolo en los dos repos que lo leen. La receta versionada es `instalador.bat`; esto era su salida. Mismo criterio que MediaTools ya aplicaba a `ShotPlayer_installer.iss`. El archivo sigue en disco y se regenera solo. [SeqChecker - El .iss generado deja de versionarse]

v0.107: Se corrige `tools/sync_version.py`, que escribia con `path.write_text(content, encoding="utf-8", newline=newline)`. El parametro `newline` de `write_text` recien existe en Python 3.10, y el `python3` del sistema en macOS es 3.9: ahi el sync fallaba entero con "write_text() got an unexpected keyword argument". Pasa a escribir con `open(newline=...)`, que acepta el parametro en todas las versiones — el mismo arreglo que LinkRedirector ya tenia documentado en su script. Se suma ademas `.git-blame-ignore-revs` con el commit de renormalizacion, por convencion con las otras apps; medido, no cambia la atribucion del blame, porque Git ya empareja solo las lineas con contenido y las unicas que quedan pegadas al commit mecanico son las vacias. [SeqChecker - sync_version compatible con Python 3.9 y blame-ignore]

v0.106: Se agrega `.gitattributes` con la politica de line endings del repo: LF por defecto y CRLF para los scripts nativos de Windows (`.bat`, `.cmd`, `.ps1`), que `cmd.exe` necesita asi para interpretar bien labels, `goto` y `call`. Sin politica cada maquina commitea con los finales de linea de su sistema, lo que produce diffs fantasma —archivos que aparecen modificados enteros sin que nadie los haya tocado— y deja el `blame` inservible. Los binarios quedan declarados explicitamente, porque uno mal clasificado como texto se corrompe al normalizarse. En `.gitattributes` gana el ULTIMO patron que matchea, asi que el default generico va primero y las excepciones despues: invertirlo rompe los `.bat` en silencio. La renormalizacion del arbol existente va en un commit aparte. Politica completa en `LGA_Base_QT_C_Py/docs/Doc_Line_Endings.md`. [SeqChecker - Politica de line endings]

v0.105: Se acelera ~2.4x el analisis sin agregar dependencias ni perder deteccion. `exrcheck` ahora corre con los flags `-t -m`, que evitan las pasadas redundantes de su modo fuzzing y validan en un solo decode, manteniendo la deteccion de truncados y chunks corruptos (validado contra frames truncados y corruptos a mitad de archivo). Ademas se elimina el segundo subproceso por frame (`exrheader`): el chequeo de consistencia de header no aportaba deteccion real frente a `exrcheck` y duplicaba el costo de spawn por frame. Decision respaldada por benchmarks en una seq real DWAA 4K (85 frames): de ~5.9s a ~2.5s @16 workers; se descartaron `oiiotool --stats` (falsos negativos: no detecta truncado ni corrupcion a mitad) y el lector in-process OIIO (limitado por GIL, mas lento que el subproceso). [SeqChecker - exrcheck -t -m y sin exrheader por frame]

v0.104: El control de CPU del footer ahora se aplica EN VIVO durante un analisis en curso, igual que MediaTools. Antes el numero de workers quedaba congelado al lanzar el proceso Python y solo cambiaba en el proximo drop. Ahora C++ escribe el limite elegido en un control-file (`cpu_control.txt` en AppData) que se le pasa al backend con `--control-file`; al mover el dropdown durante el analisis, C++ reescribe ese archivo. El backend (`LGA_SeqChecker.py`) crea el pool con un tope maximo (`--max-workers`) y limita la concurrencia real con un `DynamicGate` cuyo cupo relee un thread watcher cada 0.2s; al bajar el limite, los frames ya en proceso terminan y recien ahi se frena el resto, al subirlo arrancan mas de inmediato. Validado con test instrumentado: 8 concurrentes antes del cambio, 2 despues, sin esperar al fin del job. [SeqChecker - Control de CPU dinamico en vivo]

v0.103: Se corrige el render de la columna `Sequence` para evitar superposicion de textos y se ajusta la tabla para usar el mismo esquema de resize inteligente de MediaTools (ancho total siempre acoplado al viewport, rebalanceo manual y ajuste por contenido). Tambien se copia la logica de estado de cola del Transcode Queue (`Queued #N` / `Analyzing…`) para que solo la secuencia activa figure como analizando; el status global ahora muestra FPS de analisis calculado en tiempo real. Se replica el pipeline de estilos de MediaTools (reemplazo de tokens QSS + propiedades de app) para corregir textos negros en checkbox/combos, y en Windows el toggle `Keep this window on top` pasa a usar `SetWindowPos` sin parpadeo. [SeqChecker - UI de tabla y cola alineada a MediaTools]

v0.102: Se porta desde MediaTools_v2 el control de carga de CPU en el footer de la ventana principal (label `CPU` + dropdown de presets High/Medium/Low/Minimal) y se cablea al backend Python con `--workers` para ajustar el paralelismo real del analisis; tambien se agrega el checkbox `Keep this window on top` con persistencia de estado. En la tabla de resultados se habilita resize manual de columnas desde headers (`Interactive`), se corrige el comportamiento de `Folder` para evitar truncado permanente (scroll horizontal + tooltip de path completo), y se incorpora `ShotNameDetector` para detectar nombre de shot y colorear/intercalar prefijos en `Sequence` con los dos colores usados en MediaTools. [SeqChecker - Control CPU y mejoras de tabla]

v0.101: Se agrega el listado copiable de frames corruptos. En la tabla de resultados, la celda `Corrupt` de cada secuencia con corruptos ahora es clickeable (subrayada) y abre un dialogo con los paths completos de esos frames, seleccionables y con boton `Copy all` para enviarlos a un tercero. El texto de status final muestra el total de corruptos como link (`N corrupt frames`) que abre el listado global de todos los frames corruptos detectados. El backend Python emite los paths en `corrupt_files`/`suspect_files` dentro de `MT_SEQ_RESULT`. [SeqChecker - Listado copiable de frames corruptos]

v0.1: Version inicial de LGA SeqChecker. App Qt C++ + backend Python para detectar frames EXR corruptos. Ventana unica con drag&drop de una zona (overlay reusado de MediaTools); al soltar carpeta(s) se escanean recursivamente todas las secuencias `.exr`, se agrupan por patron `nombre.####.exr` y se analizan en tres capas: estructural (gaps + outliers de tamano), header (`exrheader`) y decode completo (`exrcheck`) en pool de workers. Resultado en tabla por secuencia con conteo OK/Suspect/Corrupt y estado coloreado. [SeqChecker - Version inicial]
